name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    uses: ringmaster/actions/.gitea/workflows/deploy-static.yml@main
    secrets: inherit
    with:
      site: dnd
      build-command: node src/build.mjs
      dist-dir: docs
